// Question

// Write a function to check if given number is odd or even

// My answer
const even_or_odd = (number) => number%2 == 0 ? 'Even' : 'Odd';

// Other answer
var evenOrOdd = n => n & 1 ? 'Odd' : 'Even'