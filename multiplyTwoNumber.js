// Question

// Write function to multiply two number


// My answer
const multiply = (num1, num2) => num1 * num2;

// Other answer
const multiplyNum = (...numbers) => numbers.reduce((acc, curr) => acc * curr);
